# ArcadeHub

A lightweight web-platform bundling multiple single-player browser games (Tic-Tac-Toe, Memory, Whack-a-Mole, Connect 4) built with HTML, CSS, JavaScript and a minimal Node.js backend.

---

## 🚀 Features

* Launch multiple HTML5 games in one unified interface (Tic-Tac-Toe, Memory, Whack-a-Mole, Connect 4)
* Pure frontend game experiences (HTML/CSS/JS) with optional Node.js server for hosting
* Modular code structure – each game has its own HTML, CSS and JS files
* Browser-friendly, lightweight and easy to deploy

---

## 🛠️ Tech Stack

* **Frontend:** HTML5, CSS3, Vanilla JavaScript
* **Backend:** Node.js (simple server setup)
* **Hosting:** Static files + optional Node.js server

---

## ⚙️ How to Run

1. Clone the repository:

   ```bash
   git clone https://github.com/nando-g/ArcadeHub-WebP.git
   cd ArcadeHub-WebP
   ```
2. (Optional) Install dependencies and start the server:

   ```bash
   cd server
   npm install
   node server.js
   ```
3. Open `index.html` in your browser (or visit [http://localhost:PORT](http://localhost:PORT) if using the server).
4. Navigate to the desired game from the homepage and start playing.

---
